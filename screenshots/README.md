# Screenshots

Drop the app screenshots named below into this folder (PNG). The README
embeds them via relative links, so keep these exact filenames.

Capture against the CURRENT UI (top-bar update-check icon, Settings with
back arrow — not older layouts). Trim each shot to the app window only.

- main-ongoing-dark.png   — main view, Ongoing tab, dark theme, one active download w/ progress bar (default theme)
- main-completed-dark.png — main view, Completed tab, dark, 2–3 finished rows, "Clear All" button visible
- quality-dropdown-dark.png — quality dropdown open on a video row, size estimates visible (`720p (410.9 MB)`)
- playlist-dark.png       — playlist expanded, scrollable video list w/ per-video progress, one row held
- settings-dark.png       — Settings screen, dark: cookies row, default format/quality, Light/Dark buttons, dependency checker rows, About card
- main-ongoing-light.png  — main view, Ongoing tab, LIGHT theme (top-bar theme toggle) — shows theming off
